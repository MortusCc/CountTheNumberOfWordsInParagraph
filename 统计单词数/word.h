#pragma once
#include <stdio.h>
#include <string.h>
#include <Windows.h>
#define LEN1 10
#define LEN2 1000000
#define MAX 100
void FindWord(char str[LEN1 + 1], char Str[LEN2 + 1]);
int IsSpace(char Str[LEN2 + 1], int i);